# A.57 (energy industry uses): a normal 5-column table (Use/Sub-use/
# Properties/Area of use/application(s)/Examples of PFAS), same shape as
# A.51/A.53/A.59/A.83 -- routed through the same extract_long_form() used
# for those rather than a bespoke positional read.
add_table(extract_long_form("A.57", "Use", "Sub-use", "Examples of PFAS"))
